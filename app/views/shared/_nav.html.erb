<nav class="navbar navbar-expand-lg" style="background-color: #A6E508;">

  <div class="navbar-brand">
    <%= link_to "Eat It", root_path %>
  </div>

  <div class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      Categories
    </a>

    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
      <% Category.all.each do |category| %>
      <li class="dropdown list"><% 'active' if params[:category] == category.name %>
        <%= link_to category.name, restaurants_path(category: category.name) %>
      </li>
      <% end %>
    </div>
  </div>

  <div class="nav navbar ml-auto">
    <%= link_to "Sign Up", new_user_registration_path %>
  </div>

  <div class="nav-signin">
    <% if user_signed_in? %>
     <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
     <% else %>
      <%= link_to "Log In", new_user_session_path %>
    <% end %> 
  </div>    

    <div class="nav navbar"> 
      <% if user_signed_in? %>
       <%= link_to "Add Restaurant", new_restaurant_path %>
      <% end %>
    </div>  

</nav>